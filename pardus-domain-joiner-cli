#!/bin/bash

TEXTDOMAIN=pardus-domain-joiner-cli
TEXTDOMAINDIR=/usr/share/locale
export TEXTDOMAIN TEXTDOMAINDIR

if [[ $UID -eq 0 ]] ; then
    exec /usr/bin/python3 /usr/share/pardus/pardus-domain-joiner-cli/src/Main.py "$@"
else
    echo "$(gettext "You must be root!")"
    exit 1
fi

